function solution(numbers, k) {
    let count = 0;
    for(let i=0;i=>0;i+=2) {
        if(i>numbers.length){
            i = i-numbers.length
            numbers[i]
        }
        count++
        if(count === k) {
           return numbers[i] 
        }
    }
}